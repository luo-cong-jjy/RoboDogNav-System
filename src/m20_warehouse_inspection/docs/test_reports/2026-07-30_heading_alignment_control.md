# M20 航向跟随与预测停车恢复测试报告

日期：2026-07-30

> 状态更新（2026-07-31）：报告所用的 SCAN 航向迟滞/减速控制已撤回，结果仅作
> 历史对照；当前主流程需在原版 SCAN + 下游运动适配链上重新复验。

## 验收目标

1. 机器人在直线和窄通道中不因机身航向落后而切向障碍；
2. 预测保护停车不能允许平移穿入膨胀区；
3. 预测停车可在纯旋转安全时恢复，当前足迹碰撞仍必须硬停车；
4. 不修改原版 SCAN 规划和可视化算法；
5. MuJoCo 障碍接触事件必须为零。

## 自动测试

```text
航向/规划/安全/系统：125 passed
MuJoCo 后端附加回归：8 passed
合计：133 passed
```

覆盖内容包括航向迟滞、最短保持、纯偏航清零、转向限速、保护区旋转正反向选择、
当前足迹禁止恢复、launch 参数接线、默认包络以及原有任务/切图回归。

## 动态试验

所有有效试验使用官方 M20 MJCF、官方 ONNX 运控、MuJoCo 动力学和原生 SCAN
B-spline 链。`collision_stop_sample_count=1` 且 `guard_events=[]` 的运行是探针启动时
收到的 latched `NOT_READY`，不计为运行期碰撞停车。

| 试验 | 关键设置 | 结果 | 用时 | 最小 guard 净空 | 预测/当前停车 | 实体障碍接触 |
|---|---|---:|---:|---:|---:|---:|
| 六目标初版航向对正 | 0.35/0.15 rad；旧 SDK 转向参数 | 6/6 通过 | 各 9.36～24.49 s | 非净空场景 | 0 / 0 | 0 |
| 单障碍初版 | 0.35/0.15 rad | 通过 | 71.42 s | -0.0037 m | 1 / 0 | 0 |
| 单障碍无恢复 | 0.55 rad；`turn_max_forward=0.05` | 预测停车死锁 | 超时 | 正净空 | >0 / 0 | 0 |
| 0.90 m 通道最终跟踪实验 | 硬半径 0.30 m；gain 1.20；爬行 0.05 | 通过 | 31.21 s | 0.0759 m | 0 / 0 | 0 |
| 单障碍纯转向边界实验 | 硬半径 0.30 m；爬行 0 | 通过 | 29.10 s | 0.2183 m | 0 / 0 | 0 |
| 六目标纯转向边界实验 | 爬行 0；旧退出阈值 0.12 rad | 5/6，否决 | 第六段 90 s 超时 | 无停车 | 0 / 0 | 0 |
| 六目标最终默认参数 | 爬行 0.05；退出阈值 0.20 rad | 6/6 通过 | 合计 108.30 s | 自由空间 | 0 / 0 | 0 |

窄通道通过证据：

```text
artifacts/navigation_motion/
  2026-07-30_heading_alignment_narrow_090_tracking_v1/
    clearance_summary.json
```

该次结果：

- 最终误差 `0.1536 m`；
- 最小 SCAN 硬足迹净空 `0.1259 m`；
- 最小独立保护净空 `0.0759 m`；
- 最长指令静止 `0.305 s`；
- 20 条 B-spline；
- 预测/当前停车 `0/0`；
- 障碍接触事件和峰值力 `0 / 0 N`。

与此前同一 `0.90 m` 基准的约 `0.0523 m` guard 净空相比，航向修正后增加约
`23.6 mm`，说明改进来自跟踪居中，而不是缩小保护区。

## 否决实验

### 增大 SCAN 硬半径

`0.32 m` 和 `0.35 m` 均在 `0.05 m` GridMap 中量化到下一圈占据单元，使 `0.90 m`
通道失去可行 A-star 路径。因此当轮实验上限取 `0.30 m`，其余 `0.15 m` 留在优化器
软距离中；正式默认值后来恢复原项目 `0.25/0.20 m` 拆分。

### 转向时完全禁止前进

`turn_max_forward=0` 的单障碍结果最好，但六目标自由导航第六段超时，且第二段路径长度
比达到 `2.72`、运动模式切换 60 次。该设置被否决。最终采用 `0.05 m/s` 低速爬行，
并把转向偏航退出阈值从 `0.12` 放宽到 `0.20 rad`，减少接近目标时长期滞留转向模式。

否决数据保留在：

```text
artifacts/navigation_motion/
  2026-07-30_heading_alignment_obstacle_turn_first/
  2026-07-30_heading_alignment_final/
```

## 最终六目标冷启动复验

证据目录：

```text
artifacts/navigation_motion/
  2026-07-30_heading_alignment_final_v2/
    navigation_motion_samples.csv
    navigation_motion_summary.json
```

| 目标 | 结果 | 用时 | 终点误差 | 最大横向偏离 | 对正事件 |
|---|---:|---:|---:|---:|---:|
| 直行 2.5 m | PASS | 9.46 s | 0.0565 m | 0.0267 m | 0 |
| 180°返回 2.5 m | PASS | 17.26 s | 0.1514 m | 0.6582 m | 2 |
| 右转 90°后前进 6 m | PASS | 24.81 s | 0.0151 m | 0.1327 m | 1 |
| 左转 90°后前进 3 m | PASS | 15.74 s | 0.1529 m | 0.1864 m | 1 |
| 180°返回 3 m | PASS | 17.81 s | 0.1523 m | 0.5810 m | 1 |
| 左转 90°返回起点 6 m | PASS | 23.23 s | 0.0704 m | 0.2191 m | 1 |

整组汇总：

- 六目标 `6/6`，合计 `108.30 s`；
- 平均/最大终点误差 `0.0998 / 0.1529 m`；
- 44 条 B-spline，54 次滚动/协调转向模式切换；
- 航向对正进入 6 次；
- 2125 个逐帧采样中，航向对正状态下 raw/safe/SDK 三层前进速度最大值均为
  `0.000000 m/s`；
- 碰撞保护停车样本 `0`；
- MuJoCo 障碍接触数、接触事件、峰值力均为 `0 / 0 / 0 N`。

两次 180°回转最大横向扫掠分别为 `0.6582 m` 和 `0.5810 m`。这反映官方轮足策略
完成调头所需的自由空间，不能把机器人视为零转弯半径质点；90°和直线目标的最大横向
偏离均不超过 `0.2191 m`。

## 当前最终参数

```yaml
controller:
  heading_slowdown_threshold: 0.15
  heading_error_threshold: 0.55
  heading_error_resume_threshold: 0.20
  heading_alignment_min_hold_sec: 0.40

navigation_adapter:
  course_yaw_gain: 1.20
  cruise_yaw_deadband: 0.02
  turn_max_forward: 0.05
  turn_yaw_exit: 0.20
  turn_min_hold_sec: 0.50

scan:
  grid_map.double_cylinder_radius: 0.25
  optimization.dist0: 0.20

collision_guard:
  footprint_radius: 0.25
  safety_margin: 0.05
  lookahead_sec: 0.70
```

## 放行状态

代码、参数接线和 133 项相关自动测试已通过；`0.90 m` 窄通道在
`turn_max_forward=0.05` 配置下动态通过，且实体接触为零。
`turn_max_forward=0.05 + turn_yaw_exit=0.20` 的六目标冷启动也已 `6/6` 通过，
不存在碰撞保护停车或实体障碍接触。该次六目标运行时使用实验性的
`0.30/0.15 m` SCAN 硬/软拆分。

随后按项目决策将 SCAN 恢复为原项目 `0.25 m` 硬半径和 `0.20 m` 软距离，独立
`0.30 m` 保护器不变。航向跟随与预测停车恢复逻辑继续放行；回退后的精确组合尚未重复
本组六目标。完整 11 步高密度巡检、重复耐久和实机净空仍属于更高层级验收。
