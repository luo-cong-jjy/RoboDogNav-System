# m20_multifloor_map

该包负责把当前活动楼层的全局 PCD 裁剪成 SCAN-Planner 使用的局部点云，并为每次地图切换维护独立的新鲜点云计数。

设计边界：

- 只处理当前 `floor_id + generation` 的地图数据。
- 收到 `FloorState.ready=false` 后立即清空缓存并发布感知未就绪。
- 新代际至少发布指定数量的局部点云后才报告就绪。
- 单帧采用轮转切片限流并可靠发布，避免大点云 UDP 分片静默丢失。
- 本包不拥有楼层切换策略，实物部署时可由真实雷达驱动替换。
