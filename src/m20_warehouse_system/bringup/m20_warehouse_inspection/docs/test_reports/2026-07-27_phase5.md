# 阶段 5 验收报告

日期：2026-07-27  
版本：0.6.0  
环境：Ubuntu 22.04 / ROS 2 Humble / CycloneDDS / RViz 平面运动学 profile

## 独立工作空间

```text
project packages copied                  7
pinned build closure                    13
base overlay before build               /opt/ros/humble only
dependency revision verification        PASS
CPU patch pre-apply hash/check           PASS
rosdep closure                          PASS
clean isolated build                    13/13 PASS
registered test cases                   209
errors / failures / skipped             0 / 0 / 0
installed config and asset validation   PASS
installed interface inspection          PASS
installed launch argument loading       PASS
```

## 楼层切换稳定性

```text
F1↔F2 alternating switches              20/20 PASS
initial/final state                     F1 gen 1 / F1 gen 21
exact generation increment              PASS
floor and sensing generation agreement  PASS
one hold/zero window per switch          PASS
non-zero safe commands after zero gate  0
target release-pose agreement           PASS
RSS first / last / peak                 767.9 / 794.3 / 795.3 MiB
RSS growth / slope                      26.4 MiB / 0.60 MiB per iteration
```

## 完整任务稳定性

```text
five-step missions                      10/10 PASS（50/50 steps）
final floor/generation                  F2 / generation 20
node graph restart between runs         no
reverse floor transaction between runs  PASS
raw RSS first / last / peak             764.8 / 841.1 / 920.7 MiB
raw RSS growth / slope                  76.3 MiB / 5.72 MiB per iteration
raw growth / slope limits               128 MiB / 8 MiB per iteration
```

10 轮运行使用早期宽匹配 RSS 选择器，数值会包含同时运行且命令行含 `m20_` 的开发进程，
所以 raw peak 是保守上界；其仍通过原始阈值。最终选择器只统计同一 launch 的 14 个
子节点，单切层校准为 raw `729.8→744.8 MiB`、预热后 steady growth/slope
`0.0 MiB / 0.00 MiB per iteration`，并验证回归结束后自动有序关闭。

## 故障注入

```text
switch outside trigger                  safely rejected
navigation with stale generation        safely rejected
active navigation cancel                ERROR_CANCELLED
active mission stop                     ERROR_STOPPED
mission stop safety                     MISSION_HOLD + zero velocity
restart on wrong floor                  FAULT_HOLD
reverse switch + retry current           NAVIGATING recovered
post-retry stop                         ERROR_STOPPED
hold velocity violations                0
```

通过标记：

```text
PHASE5_SWITCH_STRESS_PASS
PHASE5_MISSION_STRESS_PASS
PHASE5_FAULT_INJECTION_PASS
```

本报告只证明 Humble/RViz 平面 profile 的独立构建和稳定性，不代表 Gazebo 动力学或
实机 M20 已验收。
