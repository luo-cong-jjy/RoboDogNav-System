# ======================================================================
# requirements.txt —— M20 MuJoCo RL 训练的 Python 依赖清单（中文注释版）
# 安装方式：python3 -m pip install -r requirements.txt
# 说明：本文件只新增中文注释，未改动任何原始内容
# ======================================================================

# NumPy：数组与数值计算（>=1.21）
numpy>=1.21
# MuJoCo：物理仿真引擎（>=3.0）
mujoco>=3.0
# PyTorch：深度强化学习（PPO 训练与推理，>=2.0）
torch>=2.0
# ONNX：策略模型导出（>=1.14）
onnx>=1.14
# tqdm：进度条显示（>=4.64）
tqdm>=4.64
