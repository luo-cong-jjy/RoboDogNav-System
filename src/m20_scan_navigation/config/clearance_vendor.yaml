scan_planner_node:
  ros__parameters:
    # Exact upstream SCAN-Planner clearance behaviour.
    grid_map.double_cylinder_radius: 0.25
    optimization.dist0: 0.20

m20_grid_route_planner:
  ros__parameters:
    # Retain the legacy optional A* profile unchanged.
    inflation_radius: 0.60

m20_collision_guard:
  ros__parameters:
    footprint_radius: 0.25
    footprint_offset: 0.18
    safety_margin: 0.05
